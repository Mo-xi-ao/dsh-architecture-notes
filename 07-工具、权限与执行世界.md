# 工具、权限与执行世界

## 这一章的叙事主线

模型发出一个工具请求，不等于系统已经执行。真正的安全边界存在于模型请求和本地副作用之间。本章跟随一次 Bash 或文件操作，拆开工具注册、Prompt、校验、审批、沙箱、执行和结果记录。

## 工具是能力边界

```text
Tool Registry
-> schema enters System Prompt
-> model emits tool call
-> pre-execute policy
-> execute through Provider
-> post-execute observation
-> durable tool/result
```

工具注册表回答“模型能请求什么”，执行管线回答“系统允许什么发生”。两者不能合并成一个 `execute()` 方法。

## Waterfall 与执行顺序

`tools/pre-execute`、`tools/execute`、`tools/post-execute` 是 live waterfall。监听器需要通过 `next()` 委托给后续处理者，因此审批、日志、限流和真实执行可以按顺序组合。

一个可靠的工具请求至少经过：输入校验、Scope/Restriction 过滤、权限或确认策略、Provider 执行、结果规范化和 Session 记录。模型可以建议调用什么，但不能跳过这些层。

## Scope 和 Restriction

全局注册的工具对所有 Agent 可见；scope-local 注册只属于一个 Agent。Restriction 先从全局集合中过滤工具，再合并该 scope 自己的注册。被过滤的工具不仅不出现在 Prompt，也会在执行时被当作不存在，避免模型通过伪造调用绕过可见性。

## 为什么要统一执行世界

如果 Shell 在沙箱里，而文件系统和 LSP 仍指向宿主机，Agent 看到的项目和它修改的项目就不是同一个世界。DSH 把相关 Provider 设计为共享执行边界：切换到远程 Sandbox 时，Shell、PTY 和需要同一文件视图的能力一起移动。

## 一个工具请求的失败分支

```text
schema 不通过 -> 不进入 Provider
权限拒绝 -> 记录拒绝并等待或结束
Provider 超时 -> 返回可解释错误
执行成功 -> 规范化结果并写入 Session
```

这说明 `execute()` 不是工具系统的全部。模型请求、权限判断和 Durable 记录必须位于真实副作用的两侧。

## 本章结论

Tool Pipeline 把“模型想做什么”和“系统允许发生什么”分开。权限、Scope、Sandbox 和 Provider 不是附加装饰，而是 Agent 能安全接触现实世界的条件。

源码导航：`docs/tool-execution-pipeline.md`、`docs/subsystems/tools.md`、`packages/core/tools`、`packages/shell`、`packages/sandbox`。
