# 如何注册和流转一种新的抽象状态

## 这一章的叙事主线

前面说 DSH 把状态分成 Durable、Live、Derived 和 Runtime，但真正写功能时仍会遇到一个具体问题：我要增加一种“审批状态”或“任务阶段”，应该加一个字段、注册一个 Service，还是新增一种 Session Event？

普通 Agent 的直觉通常是给 `AgentState` 增加字段：

```ts
state.approval = 'waiting'
```

这个写法不是错误，但它没有回答刷新、恢复、多 Agent 隔离、UI 订阅、并发写入和卸载问题。DSH 的新观念是：**先判断状态属于哪一个时间尺度，再决定注册点。**

## 先认识参与状态流转的组件

| English | 中文名称 | 代码角色 | 主要状态类型 |
| --- | --- | --- | --- |
| `Context` | 上下文 / 能力容器 | 承载 Service、Event 和 Effect | Runtime |
| `Service` | 服务定义 | 为 `ctx.<key>` 定义稳定能力 | Runtime |
| `Plugin` | 插件 | 安装能力并绑定生命周期 | Runtime / Configured |
| `Effect` | 可逆副作用 | 注册并返回 disposer | Live lifecycle |
| `Agent` | 智能体运行句柄 | 持有 inbox、scope、取消和当前驱动 | Live |
| `AgentRegistry` | 智能体注册表 | `ctx.agents` 中跟踪 live agents | Live registry |
| `Session` | 会话事件日志 | 追加 typed `SessionEvent` | Durable facts |
| `SessionEventMap` | 会话事件词汇表 | 声明允许写入日志的事件类型 | Durable schema |
| `SessionSurface` | 会话表面投影 | 从消息类事件计算模型可见表面 | Derived live view |
| `Reducer / Fold` | 归约器 / 折叠器 | 从事件序列计算当前状态 | Derived |
| `ToolRegistry` | 工具注册表 | `ctx.tools` 中注册全局或 scoped tool | Live registry |
| `Waterfall Event` | 瀑布事件 | `next()` 委托式控制链 | Live control |
| `Remote` | 远程方法合同 | 将明确方法暴露到 Client | Wire contract |

这张表最重要的不是翻译，而是“主要状态类型”。同一个功能往往会同时经过多种组件，但每个组件只负责自己那一类状态。

## 第一步：判断状态属于哪个领域

假设要增加“工具执行等待人工批准”的能力，可以先问五个问题：

1. 页面刷新后，是否还要知道批准已经发生？
2. 恢复 Session 时，模型是否必须看见批准结果？
3. 它是否只用于阻塞当前一次工具调用？
4. 不同 Agent 是否应该拥有独立状态？
5. 插件卸载后，监听器和注册是否应该消失？

由此可以拆出三层：

```text
Live approval request
-> 当前 Promise、AbortSignal、等待中的调用

Durable approval fact
-> approved / rejected，以及它对应哪个 callId

Derived approval view
-> UI 中当前显示 waiting / approved / rejected
```

`waiting` 可能来自“存在一个 call 但没有终态事件”，它不一定需要作为独立 Durable Event；`approved` 如果会影响恢复后的模型历史，则需要稳定事实。状态不是看名字决定，而是看恢复语义决定。

## 第二步：注册一个新的 Service 抽象

当多个插件需要共享同一能力，并且实现可能被替换时，应该先定义 Service。下面是教学简化代码，API 形态对应 DSH 中 `ShellExecutor extends Service`、`AgentRegistry` 等模式：

```ts
import { Context, Service } from '@deepseek-ai/cordis'

export abstract class ApprovalService extends Service {
  static inject = []

  constructor(ctx: Context) {
    super(ctx, 'approvals')
  }

  abstract request(input: {
    agentId: string
    callId: string
    summary: string
    signal: AbortSignal
  }): Promise<'approved' | 'rejected'>
}

declare module '@deepseek-ai/cordis' {
  interface Context {
    approvals: ApprovalService
  }
}
```

这里定义的是能力词汇，不是状态存储。`ApprovalService` 说明 Consumer 怎样提出请求，但没有规定批准来自终端、Web UI 还是企业审批系统。

普通 Agent 往往直接 import `confirm()`；DSH 先定义 Service，是为了让 Provider 可以替换，Consumer 不必知道界面和传输细节。

## 第三步：由 Provider 注册实现

Provider 负责 live 等待、取消和外部交互。教学示意如下：

```ts
export class WebApprovalProvider extends ApprovalService {
  async request(input: ApprovalInput) {
    input.signal.throwIfAborted()
    return await this.waitForBrowserDecision(input)
  }
}

export function apply(ctx: Context) {
  ctx.plugin(WebApprovalProvider)
}
```

Provider 内部可能维护 `Map<callId, PendingRequest>`。这张 Map 是 Live Runtime State：进程退出后不能仅靠它恢复批准历史，也不应该直接成为 UI 的永久事实来源。

运行时状态可以画成：

```text
unregistered
-> provider mounted
-> request pending
-> approved / rejected / aborted
-> request removed
-> provider disposed
```

其中 `mounted/pending/disposed` 是教学流程阶段，不一定对应源码中的字符串枚举。真正需要跨进程保留的结果，要进入 Session Event。

## 第四步：扩展 Durable Event 词汇

DSH 的 `Session` 是 append-only typed log。新增模型可见或恢复必需的事实，应扩展 `SessionEventMap`，而不是修改旧事件或直接覆盖 Session 当前状态。

教学简化代码：

```ts
declare module '@deepseek-ai/dsh-session' {
  interface SessionEventMap {
    'approval/decided': {
      callId: string
      decision: 'approved' | 'rejected'
      reason?: string
    }
  }
}
```

随后在决定产生时追加事件：

```ts
session.append('approval/decided', {
  callId,
  decision,
  reason,
})
```

实际开发时还必须检查该事件是否属于 `SurfaceEventType`。只有会产生模型消息的事件才进入 surface，并且要声明它如何加入或替换表面。仅用于 UI 和审计的 Durable Event 可以留在日志中，但不进入模型历史。

这体现了一个重要区别：**Durable 不自动等于 Model-visible。** 所有 Model-visible 内容必须 Durable，但不是所有 Durable 事实都应该发给模型。

## 第五步：注册 Live Event 作为控制点

如果批准需要发生在工具执行之前，可以使用工具 pipeline 的 live extension point，而不是让 Session Reducer 去阻塞执行。

```ts
ctx.on('tools/pre-execute', async (payload, next) => {
  const decision = await ctx.approvals.request({
    agentId: payload.agent.id,
    callId: payload.call.id,
    summary: payload.call.name,
    signal: payload.signal,
  })

  if (decision === 'rejected') {
    return rejectedToolResult(payload.call)
  }

  return next()
})
```

Live Waterfall 的状态流是：

```text
request entered
-> current listener owns control
-> reject OR call next()
-> downstream executor
-> result returns upstream
```

它适合表达“现在是否继续”。Session Event 适合表达“刚才已经决定了什么”。一个负责控制，一个负责事实，不能互相替代。

## 第六步：通过 Reducer 生成查询状态

UI 不应该遍历所有事件后到处写临时判断。可以为 Approval 建立一个 Projection：

```ts
type ApprovalView = Record<string, {
  status: 'waiting' | 'approved' | 'rejected'
  reason?: string
}>

function reduceApproval(
  state: ApprovalView,
  event: SessionEvent,
): ApprovalView {
  if (event.type === 'tool/call') {
    return {
      ...state,
      [event.data.callId]: { status: 'waiting' },
    }
  }

  if (event.type === 'approval/decided') {
    return {
      ...state,
      [event.data.callId]: {
        status: event.data.decision,
        reason: event.data.reason,
      },
    }
  }

  return state
}
```

这里的 `waiting` 是从 `tool/call` 推导出来的，`approved/rejected` 来自终态事实。刷新页面后，Reducer 对同一事件序列重放，就能得到相同视图。

Reducer 应保持纯粹：不弹窗、不执行工具、不写日志。否则 replay 一次就可能重复触发副作用。

## 第七步：让状态属于正确的 Agent Scope

DSH 的 scope 是扁平的 per-agent registration。全局工具对所有 Agent 可见，scope-local tool 只属于一个 Agent；Restriction 过滤全局工具后，再合并本 scope 的注册。

例如只给某个 Agent 注册审批后的发布工具：

```ts
agent.ctx.tools.register(defineTool({
  name: 'deploy',
  description: 'Deploy the current workspace after approval.',
  // schema and execute omitted
}))
```

它的生命周期同时具有两种含义：

- **可见性**：只有这个 Agent 的 scope 能看到工具。
- **所有权**：Agent scope 卸载时，工具注册一起撤销。

普通 Agent 常用 `if (agentId === ...)` 判断权限；DSH 用 scope 把可见性和生命周期绑定到同一个注册事实，减少“Prompt 看不见但执行接口仍能调用”的不一致。

## 第八步：用 Effect 保证撤销

注册方法通常返回 disposer，并由 Cordis Effect 管理：

```ts
const dispose = ctx.tools.register(tool)

// plugin/scope unload 时调用；真实代码通常交给 Effect 生命周期
dispose()
```

仓库中的 Registry 实现大量使用 `ctx.effect(...)`，测试也明确验证 `register()` 返回可调用的 exact effect disposer。

状态流因此不止有业务状态，还包含注册状态：

```text
absent
-> registered
-> visible and callable
-> disposing
-> absent again
```

这是 DSH 相比传统 Agent 很新的观念：**状态不仅是业务对象里的字段，能力是否存在本身也是受生命周期管理的状态。**

## 完整流转图

```text
Bundle / Plugin mounted
-> Approval Service Provider registered        [Runtime]
-> tools/pre-execute listener registered       [Live control]
-> Agent requests tool
-> tool/call appended                           [Durable]
-> approval request pending                     [Live]
-> user decides
-> approval/decided appended                    [Durable]
-> waterfall delegates or rejects               [Live control]
-> tool/result appended                          [Durable]
-> Approval Projection folds events             [Derived]
-> Client renders current status                 [Derived UI]
-> plugin unload disposes provider/listener      [Lifecycle]
```

同一个“审批功能”经过了五种状态域，但每个状态只有一个清楚的所有者。

## 相比传统 Agent，新在哪里

### 传统 Agent：状态是对象字段

```text
AgentState {
  messages,
  tools,
  approval,
  currentTool,
  uiStatus
}
```

优点是直观、容易开始。问题是持久化、控制流、UI 和能力注册混在一起。字段改变后，很难知道谁拥有它、是否需要记录、刷新后如何恢复。

### DSH：状态是多个领域之间的协议

```text
Config 决定装什么
Runtime Context 决定现在有什么能力
Live Event 决定现在如何继续
Session Event 记录已经发生什么
Projection 决定当前如何查看
Effect 决定何时撤销
```

新观念不是“多写几个事件”，而是把状态的时间尺度和权威来源变成显式设计。它带来恢复、替换、隔离和可测试性，代价是增加事件词汇、Projection 和生命周期契约。

## 新增状态时的检查清单

1. 这是事实、控制、缓存、配置还是派生视图？
2. 进程重启后是否必须存在？
3. 模型恢复后是否必须看见？
4. 它属于一个 Agent、一个 Session，还是整个 Context？
5. 谁可以写，谁只能读？
6. 并发事件到达时，顺序是否影响结果？
7. Reducer 重放是否幂等？
8. 插件卸载时，注册、监听和 pending operation 如何清理？
9. Client 需要 unary Remote，还是 event/projection protocol？
10. 是否已有现成的 Service 或 Event seam 可以深化，而不是新造平行系统？

## 本章结论

在 DSH 中新增一种状态，不应从“给哪个对象加字段”开始，而应从“它属于哪个状态领域”开始。Service 定义能力，Provider 持有 live implementation，Waterfall 控制当前流程，Session Event 保存事实，Projection 计算视图，Scope 和 Effect 管理可见性与生命周期。

这就是它相对普通 Agent 更成熟的地方：它不只关心 Agent 能不能完成一次任务，还关心任务中途刷新、失败、恢复、替换 Provider、卸载插件以后，系统能否继续解释自己。

源码导航：`packages/core/session`、`docs/subsystems/session.md`、`packages/core/agent`、`docs/subsystems/core.md`、`packages/core/tools`、`packages/shell/shell`、`packages/shell/shell-env/src/index.ts`、`docs/agent-lifecycle.md`。
